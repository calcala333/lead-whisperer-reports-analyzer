services:
  web:
    build: .
    image: active-orders-of-protection:latest
    container_name: aop-web
    restart: unless-stopped
    ports:
      - "6900:6900"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:6900/"]
      interval: 30s
      timeout: 5s
      retries: 3

Docker build fixes applied

1. Replaced npm ci with npm install in both Docker stages.
   The existing package-lock.json was older than package.json and npm ci refused to continue.
   npm install reconciles the lock data during the image build.

2. Updated the Express 5 SPA fallback route from * to /{*splat}.
   Express 5 requires named wildcard routes.

Build commands:
  docker compose down
  docker compose build --no-cache
  docker compose up -d
  docker compose ps
  docker compose logs -f web
